spring:
  application:
    name: vela-pos

  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/vela_pos}
    username: ${DB_USERNAME:vela}
    password: ${DB_PASSWORD:vela}

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
    show-sql: false

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

vela:
  security:
    jwt:
      secret: ${JWT_SECRET:change-this-dev-only-secret-key-please-override-in-prod-min-32-bytes}
      expiration-minutes: ${JWT_EXPIRATION_MINUTES:480}

springdoc:
  swagger-ui:
    path: /swagger-ui.html

server:
  port: ${SERVER_PORT:8080}

logging:
  level:
    com.vela.pos: DEBUG
    org.hibernate.SQL: DEBUG
